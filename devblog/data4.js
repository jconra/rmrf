window.LAB4 = { html: `
<h3 style="color:#6ab7ff;font-size:14px;letter-spacing:1px;margin:2px 0 6px">Sweep 9 — threat afterglow verified (with an honest residual)</h3>
<p style="font-size:13px;margin:8px 0">The ×98-in-101s strobe dropped to <b>×21 over 75s</b>, then ~×7 bursts —
flip rate cut ~5× (once a second → once per ~3s), message repeats ×158 → ×48. The residual cadence matches the
2.5s glow window exactly, which is informative: that lurcher genuinely <i>cannot see</i> its turret from where
it sits for seconds at a time — it's a Hunter sieging with a lurcher pop-gun from a spot 110u short of its
standoff, and WHY it never walks there is the next question, not more hysteresis.</p>
<p style="font-size:13px;margin:8px 0">Elsewhere sweep 9 held the gains: 3/6 resolved (39 · 67 · 81 — 81 again
the cleanest match on record), seed 25's staring contest still gone. Resolution keeps oscillating 3–4/6 sweep
to sweep — the per-seed chaos is bigger than the remaining fixes, which is itself a sign the gross
pathologies are thinning out.</p>
` };
