VERDICT — 2026-07-17 Driver slice 2 (+ the deterministic-tournament instrument)
================================================================================

Batch (uncommitted in /rmrf/, on top of slice 1):
  1. Grind detector — 4th watchdog class (pin / orbit-stall / partial-freeze / GRIND =
     actual ground under 15% of commanded×nominal speed). Alarms reason-tagged.
  2. Reverse cap — every chassis backs at 0.5 throttle (maneuver tool, not travel);
     omni Lurcher exempt. RR.setReverseCap knob.
  3. Siege break-off — dueling while a tower is ACTIVELY HITTING us (view.towerFire,
     ~2.5s window) → slip the tower's guns while the nose tracks the rival (Jotun gets
     its reverse-arc free from locomote's goto/face split). The first version triggered
     on tower PROXIMITY and interrupted every siege of a defended base into an endless
     field duel: exact-gate showed 8 seeds flipped stalemate, towers untouched 1000s,
     kills tripled. Fired-upon trigger recovered 4 of the 8 immediately.
  4. Handle "no" — pursue: an unreachable ghost contact is written off (state collapses
     to the mission). suppress: unreachable radial standoff → bounded alternating search
     (±0.5/±0.9/±1.3 rad around the ideal line; never walks into the far corners' arcs —
     the unbounded version did, and died there). RR.setStandRot knob.
  5. budgetHit — astar/planPath now say WHY a path is partial: search budget exhausted
     (goal may be fine, just far) vs open set emptied (provably unreachable). The driver
     only convicts orders on the second. Acting on budget partials was a false-conviction
     spree (reachable contacts written off, good stands rotated away): violations counted
     ~29/match; the true number is ~10.
  6. Violations by ISSUER telemetry (suppress/pursue/advance named per match).

THE INSTRUMENT (the real story of this slice):
  Same-build tournaments differed by ±2-3 resolutions and ±46% idle-at-goal (21 vs 23
  resolved on identical code). Sources found with det-hunt.cjs (positions checksummed
  every 100 ticks): the archetype draw ran off unseeded Math.random (same seed ≠ same
  war), and a wall-clock-random number of live rAF frames advanced the sim before and
  during the driven loop. Cure: &dseed=&rngseed= (both already existed in main.js,
  never passed by the rigs) + rig-side kit (performance.now frozen and rAF dead from
  t=0 via addInitScript). Result: BIT-IDENTICAL matches; whole tournaments byte-diff
  clean; per-seed before/after diffs; bisection on just the flipped seeds in ~12 min.
  Every historical tournament verdict carried that noise floor.

EXACT GATE (deterministic, seeds 11+7k, per-seed diffable):
                                BEFORE (slice-1 HEAD)   AFTER (refined slice-2)
  resolved @1000s horizon       26/30                   23/30
  resolved @1500s horizon       28/30                   27/30
  hard stalemates @1500s        2 (186,214)             3 (74,95,151)
  avg resolve                   467s                    527s
  contract violations           705 (inflated)          293 (honest count)
  nav alarms                    19 (0.63/m)             21 (0.70/m)

Reading: at the honest horizon the builds are ONE seed apart — inside the ±4-5-seed
chaos reshuffle that ANY behavior change causes (deterministic ≠ attribution: 4 of the
before-build's stales RESOLVE under slice 2). The real, systematic cost is PACE: sane
behaviors take longer (break off under fire, half-speed reverses, stand searches), so
more endgames cross 1000s (4 vs 2). Per the standing rule — smarter decisions beat
win-rate — that trade is accepted.

VERDICT: KEEP (with the fired-upon break-off, bounded stand search, and budgetHit
honesty that the exact instrument forced). Bisection knobs stay in (RR.setReverseCap /
setStandRot / setNavScuttle). Known follow-ups: 3 hard stales (74/95/151) are the next
autopsy cases; tournament horizon may deserve 1200-1500s now that endgames are honest.
